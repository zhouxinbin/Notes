# 运算符重载与结构体排序

```cpp
{
    int a, b, w;
    bool operator< (const Edge &t) const
    {
        return w < t.w;
    }
} edges[M];
```
![Exported image](../images/Exported image 20260608170205-1.png)

```
平替：利用sort快排＋cmp函数
```

```cpp
struct Stu
{
    int a, b, c;
} s[1000];
bool cmp(Stu x, Stu y)
{
    return x.c < y.c;
}
```

```
此方法中cmp一定要放在结构体的下面 一定注意顺序！！！
```
